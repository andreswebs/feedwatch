// Package sqlite implements the store.Store interface over a pure-Go SQLite
// database (modernc.org/sqlite, no CGO), including the embedded versioned
// migrations and the schema-version guard.
package sqlite
