---
name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    name: build & publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          # Full history and tags so `git describe` in the Makefile resolves
          # correctly, matching the VERSION we pass in below.
          fetch-depth: 0

      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: src/go.mod
          cache-dependency-path: src/go.sum

      - name: Install golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          version: v2.12.2
          install-only: true

      - name: Build distribution archives
        run: make dist VERSION="${GITHUB_REF_NAME}"

      # cosign-installer v4 installs cosign v3.x, which writes the standardized
      # Sigstore bundle format (.sigstore.json). Consumers need cosign v3+ to
      # verify.
      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Generate SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        env:
          SYFT_SOURCE_NAME: feedwatch
          SYFT_SOURCE_VERSION: ${{ github.ref_name }}
        with:
          path: src
          format: spdx-json
          output-file: dist/feedwatch-${{ github.ref_name }}.spdx.json
          upload-artifact: false

      - name: Sign checksums
        run: cosign sign-blob --yes --bundle dist/SHA256SUMS.txt.sigstore.json dist/SHA256SUMS.txt

      - name: Sign SBOM
        run: |
          cosign sign-blob --yes \
            --bundle "dist/feedwatch-${GITHUB_REF_NAME}.spdx.json.sigstore.json" \
            "dist/feedwatch-${GITHUB_REF_NAME}.spdx.json"

      - name: Attest build provenance
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-checksums: dist/SHA256SUMS.txt

      - name: Create GitHub release
        uses: ./.github/actions/gh-release
        with:
          tag: ${{ github.ref_name }}
          repo: ${{ github.repository }}
          github-token: ${{ github.token }}
